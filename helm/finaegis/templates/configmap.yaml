apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "finaegis.configMapName" . }}
  labels:
    {{- include "finaegis.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
